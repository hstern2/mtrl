# mtrl

Molecular generation with AMSR. Depends on `trl` (token-sequence transformer + RL) and `amsr` (molecular string representation). Provides AMSR data prep, molecular objectives, and evaluation.
